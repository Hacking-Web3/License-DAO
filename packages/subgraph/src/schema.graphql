type MemberProposal @entity {
  id: ID!
  address: Bytes!
  ipfsHash: String!
  createdAt: BigInt!
  transactionHash: String!
  status: String!
}

type JoinedUser @entity {
  id: ID!
  address: Bytes!
  createdAt: BigInt!
}